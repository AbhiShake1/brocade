import React from 'react';
import type {NextComponentType} from "next";

const Header : NextComponentType = () => {
    return (
        <header className='w-full h-64 bg-red-900'>

        </header>
    );
};

export default Header;